
function ChainageStart(id) {
    var element = document.getElementById(id);
    var regex = /[^0-9.]/gi;
    element.value = element.value.replace(regex, "");
}
function chainageEnd(id) {
    var element = document.getElementById(id);
    var regex = /[^0-9.]/gi;
    element.value = element.value.replace(regex, "");
}
function progressInTotalMeter(id) {
    var element = document.getElementById(id);
    var regex = /[^0-9.]/gi;
    element.value = element.value.replace(regex, "");
}
function progressInPercentage(id) {
    var element = document.getElementById(id);
    var regex = /[^0-9.]/gi;
    element.value = element.value.replace(regex, "");
}

function fsegSegmentNumber(id) {
    var element = document.getElementById(id);
    var regex = /[^0-9.]/gi;
    element.value = element.value.replace(regex, "");
}

function l1segSegmentNumber(id) {
    var element = document.getElementById(id);
    var regex = /[^0-9.]/gi;
    element.value = element.value.replace(regex, "");
}
function b1segSegmentNumber(id) {
    var element = document.getElementById(id);
    var regex = /[^0-9.]/gi;
    element.value = element.value.replace(regex, "");
}
function b2segSegmentNumber(id) {
    var element = document.getElementById(id);
    var regex = /[^0-9.]/gi;
    element.value = element.value.replace(regex, "");
}
function b3segSegmentNumber(id) {
    var element = document.getElementById(id);
    var regex = /[^0-9.]/gi;
    element.value = element.value.replace(regex, "");
}
function b4segSegmentNumber(id) {
    var element = document.getElementById(id);
    var regex = /[^0-9.]/gi;
    element.value = element.value.replace(regex, "");
}
function b5segSegmentNumber(id) {
    var element = document.getElementById(id);
    var regex = /[^0-9.]/gi;
    element.value = element.value.replace(regex, "");
}
function l2segSegmentNumber(id) {
    var element = document.getElementById(id);
    var regex = /[^0-9.]/gi;
    element.value = element.value.replace(regex, "");
}

